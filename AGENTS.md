# 🏃 Workout & Tennis Journal Apps Script Project

이 프로젝트는 **종합 운동일지**와 **테니스 일지**를 구글 스프레드시트 및 구글 드라이브와 연동하여 관리하는 웹앱(Google Apps Script) 모노레포입니다.
안티그래비티(Antigravity)와 협업할 때 이 문서를 기준으로 모든 맥락과 데이터 스키마를 파악합니다.

---

## 📁 프로젝트 폴더 구조

```text
workout-log-app/
├── AGENTS.md                         # 안티그래비티 프로젝트 컨텍스트 및 AI 메모리
├── .gitignore                        # Git 무시 파일 목록
├── .agents/
│   ├── hooks.json                    # 안티그래비티 작업 완료 시 아이폰 푸시 훅
│   └── scripts/
│       └── notify.py                 # ntfy 푸시 발송 파이썬 스크립트
├── google_apps_script/               # 🏃 종합 운동일지 앱
│   ├── Code.gs                       # 백엔드 (시트 DB, 드라이브 다중사진, 댓글, ntfy, 정렬)
│   └── index.html                    # 프론트엔드 (기록폼, 일지목록/수정모달, 댓글UX, 통계차트)
└── tennis_apps_script/               # 🎾 테니스 일지 앱
    ├── Code.gs                       # 백엔드 (테니스 DB, 드라이브 사진, 전적집계, 댓글, ntfy)
    └── index.html                    # 프론트엔드 (레슨/클럽게임/연습폼, 스와이프갤러리, 댓글UX, 승률차트)
```

---

## 📊 데이터베이스 (Google Spreadsheet) 스키마

### 1. 종합 운동일지 (`google_apps_script/`) - 17개 열 (A~Q)
* **시트명**: `운동일지`
* **열 구성**:
  1. `A (1)`: **날짜** (`YYYY-MM-DD`)
  2. `B (2)`: **운동종목** (런닝, 등산, 트레일런닝, 프리다이빙, 걷기, 기타)
  3. `C (3)`: **제목**
  4. `D (4)`: **운동시간(분)**
  5. `E (5)`: **운동강도(RPE)** (1~10)
  6. `F (6)`: **거리(km)**
  7. `G (7)`: **페이스** (예: `5'30"`)
  8. `H (8)`: **획득고도(m)**
  9. `I (9)`: **최고고도(m)**
  10. `J (10)`: **수심(m)**
  11. `K (11)`: **종목세부/기타종목**
  12. `L (12)`: **장소/코스**
  13. `M (13)`: **날씨** (맑음, 구름조금, 흐림, 비, 실내)
  14. `N (14)`: **장비**
  15. `O (15)`: **메모/후기**
  16. `P (16)`: **사진ID** (Google Drive 파일 ID 쉼표 구분)
  17. `Q (17)`: **댓글** (JSON 배열: `[{"id":"uuid", "author":"작성자", "text":"내용", "createdAt":"...", "updatedAt":"..."}]`)

### 2. 테니스 일지 (`tennis_apps_script/`) - 14개 열 (A~N)
* **시트명**: `테니스일지`
* **열 구성**:
  1. `A (1)`: **날짜** (`YYYY-MM-DD`)
  2. `B (2)`: **활동구분** (레슨, 클럽게임, 랠리/연습)
  3. `C (3)`: **제목**
  4. `D (4)`: **소요시간(분)**
  5. `E (5)`: **운동강도(RPE)**
  6. `F (6)`: **경기결과** (승, 패, 무, 연습)
  7. `G (7)`: **세트스코어** (예: `6-4`)
  8. `H (8)`: **매치상대_파트너**
  9. `I (9)`: **코트종류** (인조잔디, 하드코트, 클레이, 실내코트)
  10. `J (10)`: **구장위치** (문수, 중구, 야음, 남산, 동서, 유곡, 직접입력)
  11. `K (11)`: **중점기술** (포핸드, 백핸드, 발리, 서브 등)
  12. `L (12)`: **코치피드백_복기메모**
  13. `M (13)`: **사진ID** (Google Drive 파일 ID 쉼표 구분)
  14. `N (14)`: **댓글** (JSON 배열)

---

## 🔔 ntfy 스마트폰 푸시 알림 설정

* **종합 운동일지 토픽**: `my-workout-log-7788` (운동 기록 및 피드백 댓글 도착 시 발송)
* **테니스 일지 토픽**: `my-tennis-log-7788` (테니스 기록 및 피드백 댓글 도착 시 발송)
* **안티그래비티 작업 완료 알림 토픽**: `my-gravity-7788` (안티그래비티 코딩/답변 완료 시 아이폰 푸시)

---

## ⌚ 스마트워치 연동 (Amazfit T-Rex 3 Pro & Zepp App)
* **지원 포맷**: `GPX`, `TCX`, `XML`
* **동작 방식**: Zepp 앱에서 '내보내기'한 GPX 또는 TCX 파일을 웹앱 폼의 `[📂 GPX/TCX 선택]` 영역에 드롭/선택하면, 브라우저가 순수 자바스크립트로 0.1초 만에 파싱하여 **날짜, 소요시간, 종목(런닝/등산/트레일/걷기 자동 감지), 이동거리(km), 평균 페이스, 획득고도(m), 최고고도(m), 심박수/칼로리**를 폼에 자동 기입합니다.

---

## 🚀 배포(Deploy) 가이드
1. 구글 스프레드시트 ➔ **`[확장 프로그램]` ➔ `[Apps Script]`**
2. 해당 프로젝트 폴더의 `Code.gs`와 `index.html` 복사/붙여넣기 후 **`Ctrl + S` 저장**
3. 우측 상단 **`[배포]` ➔ `[배포 관리]` ➔ 연필 ✏️ ➔ 버전: `[신규 버전]` ➔ `[배포]`**

